<!--html > cotainer直下でないと高さがないため反映されない-->
<div id='map'></div>
<%= hidden_field_tag(:center_latitude, @center_lat) %>
<%= hidden_field_tag(:center_longitude, @center_lng) %>
<% @courts.each do |court| %>
 <%= hidden_field_tag(:latitude, court.latitude, {class: 'latitudes'}) %>
 <%= hidden_field_tag(:longitude, court.longitude, {class: 'longitudes'}) %>
<% end %>