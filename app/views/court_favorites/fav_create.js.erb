document.getElementById('favorite').innerHTML = "<%= escape_javascript(
  link_to('お気に入り追加', court_favorites_path(id: court_id), method: 'post', remote: true, class:'btn btn-sm text-white bg-orange small'
)) %>";
alert('お気に入りから削除しました')